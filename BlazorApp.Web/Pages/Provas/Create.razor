<!-- page "/provas/cadastrar"
@inherits CreateProvaPage

<MudContainer MaxWidth="MaxWidth.Medium">
    <MudText Typo="Typo.h4" Class="mt-10 mb-6">Nova Prova</MudText>

    <EditForm Model="@InputModel" Context="ProvaForm" OnValidSubmit="OnValidSubmitAsync">
        <MudPaper Class="pa-8" Outlined="true">
            <ObjectGraphDataAnnotationsValidator />
            <DataAnnotationsValidator />
            <MudTextField Label="Curso Id"
                          HelperText="Campo obrigatório"
                          @bind-Value="InputModel.CursoId"
                          For="@(() => InputModel.CursoId)" />

            <MudTextField Label="Questão"
                          HelperText="Inclua uma questão"
                          @bind-Value="Questao" 
                          Immediate="true" 
                          ImmediateInterval="500" 
                          OnKeyUp="OnKeyUp"/>
            <MudList>
                @foreach (var result in Questoes)
                {
                    <MudListItem>@result</MudListItem>
                }
            </MudList>

        </MudPaper>

        <MudButton ButtonType="ButtonType.Submit"
                   Variant="Variant.Filled"
                   Color="Color.Primary"
                   Class="mt-8 mb-10">
            Cadastrar
        </MudButton>
    </EditForm>
</MudContainer>--> -->