@page "/cadastrar"
@inherits CreateUsuarioPage

<OtherValue User="@Logged">
    
    <MudText Typo="Typo.h4" Class="mt-10 mb-6">Nova Empresa</MudText>

    <EditForm Model="@InputModel" Context="UsuarioForm" OnValidSubmit="OnValidSubmitAsync">
            <DataAnnotationsValidator />

        <MudTextField Label="Nome"
                      HelperText="Campo obrigatório"
                      @bind-Value="InputModel.Nome"
                      For="@(() => InputModel.Nome)" />

        <MudTextField Label="Email"
                          HelperText="Campo obrigatório"
                          @bind-Value="InputModel.Email"
                          For="@(() => InputModel.Email)" />

        <MudTextField Label="Senha"
                      HelperText="Campo obrigatório"
                      @bind-Value="InputModel.Senha"
                      For="@(() => InputModel.Senha)" />
    </EditForm>
</OtherValue>

@code
{
    public string? Logged = null;
}